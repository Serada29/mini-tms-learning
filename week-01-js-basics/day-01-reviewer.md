const truckId = "TRK-001"
let truckStatus = "Available"

# truckId is const because a truck’s identity should never change.

# truckStatus is let because status represents state, and state changes over time.
